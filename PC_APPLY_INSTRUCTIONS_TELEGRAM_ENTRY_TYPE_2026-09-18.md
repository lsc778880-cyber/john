# PC 적용 지시문: 텔레그램 리버스/추세형 구분 메시지

## 적용 목적

금일 PC 버전에 적용할 변경은 텔레그램 진입 알림의 **리버스/추세형 구분 메시지**로 한정한다.

다음 두 가지를 표시한다.

1. 진입유형: `추세형 (TREND)` 또는 `리버스 (REVERSAL)`
2. 진입사유: 브로커 확인 문구가 아닌 원래 전략 트리거

## 적용 대상

- PC 실전 주간 버전
- PC 실전 야간 버전
- 두 버전에서 텔레그램 진입 체결 보고를 생성하는 부분만 수정

## 적용 제외

다음 내용은 PC에 적용하지 않는다.

- KIS 모의투자 웜업 이어받기
- 웜업 체크포인트
- 웜업 재시도 횟수·백오프 변경
- REST 호가·계좌 조회 중지 로직
- `config.py` 웜업 설정
- `core/history_seed_loader.py`
- 모의버전 파일 덮어쓰기

## 구현 지시

1. 텔레그램 ENTRY 보고 생성 시 진입 상태의 `entry_regime` 또는 동등한 전략 구분값을 확인한다.
2. `TREND`는 `진입유형=추세형 (TREND)`로 표시한다.
3. `REVERSAL`는 `진입유형=리버스 (REVERSAL)`로 표시한다.
4. KIS 잔고 확인으로 체결이 확정된 경우에도 `KIS_BALANCE_CONFIRMED`, `KIS_POSITION_TAKEOVER`, `SERVER_SYNC` 같은 브로커 확인 문구를 진입사유로 보내지 않는다.
5. 주문 제출 시점에 저장한 전략 스냅샷, 주문 감사 정보, 직전 신호 사유에서 원래 진입 트리거를 복원한다.
6. 원래 전략 사유를 확인할 수 없는 경우에만 안전한 기본 문구를 사용하며, 브로커 확인 코드를 전략 사유로 위장하지 않는다.

## 메시지 예시

추세형 진입:

```text
진입유형=추세형 (TREND)
진입사유=<원래 추세형 진입 트리거>
```

리버스 진입:

```text
진입유형=리버스 (REVERSAL)
진입사유=<원래 리버스 진입 트리거>
```

## 변경 제한

- 진입·청산 조건을 변경하지 말 것.
- 주문 제출, 수량, 가격, 취소, 미체결 처리를 변경하지 말 것.
- 웜업, 시세 수신, REST 호출 주기, 재연결 로직을 변경하지 말 것.
- 모의버전의 `kis_main.py`, `config.py`, `core/history_seed_loader.py`를 실전버전에 통째로 덮어쓰지 말 것.
- App Key, App Secret, 계좌번호, 텔레그램 토큰 및 실행 로그를 변경하거나 저장소에 올리지 말 것.

## 검증 항목

1. 추세형 진입 알림에 `추세형 (TREND)`가 표시되는지 확인한다.
2. 리버스 진입 알림에 `리버스 (REVERSAL)`가 표시되는지 확인한다.
3. 잔고 확인 체결에서도 진입사유에 원래 전략 트리거가 표시되는지 확인한다.
4. `KIS_BALANCE_CONFIRMED`가 진입사유로 출력되지 않는지 확인한다.
5. 수정 전·후 전략 신호, 주문 경로, 자동매매 상태가 동일한지 확인한다.
6. Python 문법 검사와 텔레그램 보고 관련 회귀 테스트를 실행한다.

## 완료 보고 항목

- 수정한 실전 버전과 파일 목록
- 텔레그램 추세형 메시지 확인 결과
- 텔레그램 리버스 메시지 확인 결과
- 원래 진입사유 복원 확인 결과
- 매매·웜업·REST 로직 미변경 확인 결과

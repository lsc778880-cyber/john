# 한국투자증권 자동매매 최근 작업 정리

- 작업일: 2026-09-18
- 대상: ZENITH DELTA KIS 모의투자 버전
- 범위: 실시간 데이터 수신, REST 요청 제어, 주문 상태 복구, 텔레그램 체결 알림

## 1. KIS 초당 거래건수 초과 및 시세 수신 중단

### 확인된 원인

1. 같은 모의투자 앱이 중복 실행되어 동일 App Key로 REST 시세 조회가 중복 발생함.
2. KIS 모의투자 시세는 실시간 WebSocket이 아니라 REST 주기 조회로 수신하고 있어 요청 제한에 민감함.
3. 체결이 잔고로 확인된 후에도 `ORDER_MONITOR` 조회가 무한 반복되어 `QUOTE` 요청과 경쟁함.
4. REST 응답이 느려진 상태에서 자동 재연결이 반복되며 워밍업·잔고·미체결·주문가능 조회가 추가로 발생함.

### 수정 내용

- `QLockFile`을 이용한 작업 폴더별 단일 실행 보호 추가.
- 시세 조회 주기를 REST 최소 간격보다 여유 있게 조정.
- 평상시 미체결 조회 주기를 60초로 조정.
- REST 주문 보조 모니터를 최대 60초로 제한.
- 체결 콜백 또는 KIS 잔고 확인 완료 시 주문 모니터를 즉시 정리.
- 주문 조회 결과에서 일정 시간 이상 보이지 않는 주문을 자동 정리.
- KIS 연결 상태가 정상이면 REST 타임아웃만으로 API 재연결을 실행하지 않도록 수정.
- 장중 건강 재조회 최소 간격을 15초로 설정해 요청 폭주 방지.

## 2. `미확인 진입주문` 상태 고정

### 발생 흐름

1. 진입 주문 접수 후 자동취소 요청이 타임아웃됨.
2. 추가 진입을 막기 위한 안전장치가 `미확인` 상태로 전환됨.
3. 잔고·미체결 조회도 지연되어 상태 해제가 늦어짐.
4. 재연결 반복으로 추가 REST 요청이 발생해 복구를 더 느리게 만듦.

### 결과

- KIS 서버 잔고에서 포지션 없음(FLAT)이 확인되면 `ENTRY_UNKNOWN_RESOLVED_FLAT`으로 진입 보호 상태를 해제함.
- 서버 잔고에서 포지션이 확인되면 해당 포지션을 인계하고 진입 체결로 처리함.
- 불확실한 상태에서 추가 주문을 보내지 않는 안전 원칙은 유지함.

## 3. 텔레그램 진입사유 누락

### 확인된 원인

- 반전형 진입이 KIS 잔고로 확인될 때 원래 전략 사유 대신 `KIS_BALANCE_CONFIRMED` 같은 브로커 확인 문구가 텔레그램에 표시됨.

### 수정 내용

- 브로커 확인 문구를 전략 진입사유로 사용하지 않도록 필터링.
- 주문 감사 정보와 진입 제출 스냅샷에서 원래 전략 트리거를 복원.
- 텔레그램 진입 보고에 아래 형식으로 표시.
  - `진입유형=추세형 (TREND)` 또는 `반전형 (REVERSAL)`
  - `진입사유=<원래 전략 트리거>`

## 4. 모의·실전 동시 실행 확인

- 모의투자와 실전투자는 서버 주소와 App Key가 다르면 기술적으로 동시 실행 가능.
- 같은 App Key로 여러 프로세스를 실행하면 WebSocket `ALREADY IN USE` 및 REST 요청 초과가 발생할 수 있음.
- KIS 모의투자 REST 제한은 실전보다 엄격하므로 요청 주기와 중복 실행을 특히 주의해야 함.

## 5. 검증

- Python 문법 검사 완료.
- 단일 실행 보호 테스트 완료.
- KIS 주문 모니터 만료/정리 테스트 완료.
- REST 오류 시 불필요한 재연결 억제 테스트 완료.
- 텔레그램 진입유형/진입사유 복원 테스트 완료.

## 6. 운영 주의사항

- 실행 전 동일 버전의 Python 프로세스가 이미 실행 중인지 확인.
- `미확인` 상태에서는 임의로 다음 주문을 보내지 말고 KIS 잔고와 미체결 조회 결과를 우선 확인.
- 활성 포지션이 있을 때는 임의로 프로그램을 종료하지 않음.
- 재시작 후 잔고 인계, 미체결 동기화, 워밍업 완료 여부를 확인.

---

보안을 위해 계좌번호, App Key, App Secret, 텔레그램 토큰, 실제 거래 로그 원문은 이 문서에 포함하지 않았다.
